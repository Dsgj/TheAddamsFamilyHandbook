# M1 — Foundation and parity — DONE (2026-09-23)

Project: `../valvet` (sibling of the kit). Kit is read-only source; `pnpm sync-kit` copies data.

## State
- All gates green: `pnpm check` 0 errors, `pnpm lint`, `pnpm test` 11/11, `pnpm test:e2e` 16/16,
  `pnpm build` 342 pages, precache 394 entries / 1.67 MB.
- Deployed: https://dsgj.github.io/TheAddamsFamilyHandbook/ from public repo Dsgj/TheAddamsFamilyHandbook,
  branch `main`, Pages source GitHub Actions, env `github-pages` allows `main`. Deploy run green.
- Docker image untested (no Docker locally). Dockerfile copies dist to `html${BASE_PATH}`.

## Decisions (owner, §10)
English UI everywhere; name Valvet; GitHub Pages; log/photos local per device.

## Next: M2 (see kit-docs/BUILD-PROMPT.md §9)
Storage adapter (Dexie) replacing `src/lib/model/status.svelte.ts` localStorage; service log seeded
from kit-docs/MACHINE.md; photos.

## Key files
astro.config.ts · src/lib/handbook/{loader,render}.ts · src/lib/codes.ts · src/lib/shared-cause.ts ·
src/components/*.svelte · src/pages/** · tests/e2e/smoke.spec.ts · .github/workflows/deploy.yml
